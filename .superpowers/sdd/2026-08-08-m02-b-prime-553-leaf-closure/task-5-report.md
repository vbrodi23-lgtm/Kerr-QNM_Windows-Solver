# TASK-009 — Complete B′ campaign planner and PowerShell handoff

## Status

DONE AFTER FINAL REVIEW REMEDIATION, SOFTWARE-ONLY — all 553 literal B′ leaves are planned, but none of the
553-leaf scientific campaign was computed. The only execution smoke is the
frozen ten-position bundle: three authenticated recorded replays and seven
explicitly synthetic orchestration contracts.

The authenticated GSN cache is absent in this environment. No cache was
generated or downloaded, no physical kernel was invoked, no scientific atlas
exists, and linear-response provider admission remains closed.

## Delivered contract

- `src/windows_solver/response_batches.py` defines the typed exact campaign,
  canonical cohorts and selections, atomic content-digested checkpoints,
  strict cold/resume behavior, zero-work merge, exact deep-precision state
  machine, full-versus-partial validation, and the frozen ten-case smoke.
- `src/windows_solver/cli.py` exposes PowerShell-friendly `campaign-plan`,
  `campaign-run`, `campaign-resume`, `campaign-validate`, `campaign-merge`, and
  `campaign-smoke` commands. Every command emits one canonical JSON object or
  one machine-readable error object with a stable nonzero exit.
- `docs/response-replay-powershell.md` documents manifest creation, explicit
  and cohort selection, smoke preflight, cold/resume/validate, disjoint merge,
  exact-553 validation, and the required authenticated cache environment.
- The native campaign bridge authenticates the TASK-008 engine and exact GSN
  cache before work. It exposes only the installed binary64 capability; absent
  cache bytes or absent authenticated 80/120-digit capability fail closed.

## Exact plan evidence

| Contract | Value |
|---|---:|
| ordered leaves | 553 |
| primary | 441 |
| control | 48 |
| deep | 64 |
| cohorts | 15 |
| horizon admittance | 79 |
| exterior fixed-r3 | 71 |
| exterior light-ring | 87 |
| exterior throat-kappa | 87 |
| exterior alpha-zero | 71 |
| exterior alpha-half | 71 |
| exterior alpha-one | 87 |

- Campaign ID: `b-prime-campaign-80e2150845fe9e32fa37d7ecc660fa24083f2a179668b2915bc2a01b748b4f49`.
- Ordered leaf-set SHA-256:
  `9cd7b8f91ad229d33dae45654c96237a6eb04abd36886a79e00b614b1b2a2ed3`.
- Root-set SHA-256:
  `136766efd574137df4f5b5d5f10d30beb185d3ed46d2b24b1f8f57bffb0b0c26`.
- Numerical-policy SHA-256:
  `2d7cee336c6126a11bccd652ee35e73de60837e9418476849b9026cd27bf6171`.
- Binary64 capability SHA-256:
  `d44b6071c023abde5071d36187693b9e92d51c468cd5ec79b8e847f59b724813`.
- Cohort-set SHA-256:
  `3d2786db54e6582288b09f4c4a4fd5beb5f33d4a04c8c039a6f6db3c238fb641`.
- Campaign-source SHA-256:
  `bdd01cf60cfa808b8b1a1076e8bd9b51915ed2246e9b1995db4d04ad05a3e4af`.
- Default precision-factory identity:
  `windows_solver.response_batches:NativeCampaignStageBackend.from_environment`
  with module SHA-256
  `bdd01cf60cfa808b8b1a1076e8bd9b51915ed2246e9b1995db4d04ad05a3e4af`.
- TASK-008 engine-source SHA-256:
  `eafa3f7ce2a2e76f87554d1f73eb3aa4a799a87b2b6bffe8f44383e9327c0daf`.

The campaign is derived only from
`B_PRIME_RELEASE_DOMAIN.production_leaves`, in installed canonical order. No
ambient Cartesian completion occurs. Every leaf retains role, exact mode,
direct-χ or source-rational Mκ coordinate, mechanism, installed root, policy,
engine/campaign sources, backend/capability, and runtime bindings.

## Frozen ten-position smoke

| # | Literal leaf ID | Evidence kind | State | Digits |
|---:|---|---|---|---|
| 1 | `b-prime-leaf-9e5777728144433e089f9559b92b6e139e16115a5a53099f40403a45297aa3c3` | authenticated recorded replay | `PRODUCED` | 64 |
| 2 | `b-prime-leaf-7ef38d6f95c161d0b4c6650d470898c0742ad6ae8440e89956312344c0db6aac` | authenticated recorded replay | `PRODUCED` | 64 |
| 3 | `b-prime-leaf-87cd3420fc56eca66218e7414d17fd2803d59e719f3ab7c05b52a997f5cf0895` | synthetic orchestration | `PRODUCED` | 64 |
| 4 | `b-prime-leaf-65d0a36c9075d255c2e78a5d8c8098ba745da0b4228c309d210cf05bdb9696ea` | synthetic orchestration | `PRODUCED` | 64 |
| 5 | `b-prime-leaf-e0a48b72b4071c5c88c66955420dc2748cfeeac577b8fd1c399f171f5fa08475` | authenticated recorded replay | `UNRESOLVED` | 64 |
| 6 | `b-prime-leaf-0b1d66162e12c8b4e5ae0d2d360bc3f22f5f18f09e75fb84cd90d435292fed34` | synthetic orchestration | `PRODUCED` | 64 |
| 7 | `b-prime-leaf-29476bee7eab938f57ee149c682a367cd3e65f2b3337e90aeae91009998d08a2` | synthetic orchestration | `PRODUCED` | 64 |
| 8 | `b-prime-leaf-bef33f29593b50014490d255e06e90e6e0fb4b94868a7906d7d10db62522cbbd` | synthetic orchestration | `PRODUCED` | 64 → 80 |
| 9 | `b-prime-leaf-0784febf73878ce64ed23c8284325d2dc5ca30ce97cdccb543c0250371d39d7a` | synthetic orchestration | `PRODUCED` | 64 → 80 → 120 |
| 10 | `b-prime-leaf-05897ef7c7de073f4ae158d8ebce3da469eaebb0985a54bf30e8ed102c533d5f` | synthetic orchestration | `MISSING_PRECISION` | 64 |

This is exactly the brief's canonical head, two risk-bearing primary positions,
low-signal primary, recorded adverse tail, two control role gates, fixed deep
sentinel, conditional 120-digit deep case, and canonical deep tail. No other
smoke leaf was added. Synthetic records are labelled contract evidence and are
never treated as scientific output.

## Checkpoint, merge, and precision evidence

- Empty, duplicate, off-domain, reordered, or cross-role selections are
  rejected before backend calls.
- A synthetic interruption committed the first of two selected stages. Resume
  reused one and executed one; a complete warm resume reused both with zero
  backend calls.
- Checkpoints reject duplicate keys, nonfinite values, stale bindings, changed
  stage data, changed record content, or resealed outer digests.
- Merge accepts disjoint or byte-identical records and performs zero backend
  work. It rejects disagreeing overlaps and stale/mixed plan bindings.
- Relative-path resolution rejects POSIX/Windows absolute paths, drive and
  drive-relative paths, UNC, ADS, traversal, symlink crossing, and symlink
  escape before work.
- The deep state machine records all seven required diagnostic channels and
  applies only the frozen gates. The fixed sentinel executes 64 → 80; the
  unenclosed provisional case executes 64 → 80 → 120; missing authenticated
  precision yields `MISSING_PRECISION`; missing diagnostics fail closed.
- Partial validation remains `release_admissible=false`. Full validation
  requires the exact ordered 553 leaves with terminal precision evidence and
  rejects any missing/extra leaf. A computed governed `UNRESOLVED` leaf is
  complete, while `MISSING_PRECISION` is not.

## Review remediation

- Checkpoint loading now reconstructs every production `ComponentResult` and
  binds its job, leaf, mechanism, installed root/readouts, policy, backend,
  equation, sampling coordinate, numerical state, and baseline root to the exact
  campaign leaf. Recomputing all content digests cannot make swapped lineage
  valid.
- Deep loading recomputes the frozen binary64 trigger IDs, exact sentinel
  identity, mandatory 80-digit sentinel stage, conditional 120-digit stage,
  comparison thresholds, discrepancy enclosure, and record state. Resealed
  sentinel-64-only and CONVERGED/result-state inconsistencies are rejected.
- `MISSING_PRECISION` and invalid-sentinel records keep the completed stage
  prefix and the checkpoint remains `PARTIAL`. A same-plan, same-identity
  backend exposing a superset of currently available precision stages resumes
  only the missing stage; completed terminal leaves remain zero-work.
- CLI selection and merge manifests accept an exact authenticated precision
  descriptor containing `module:function`, safe relative module path, pre-import
  module SHA-256, full backend identity, and advertised capabilities. `plan` and
  `validate` authenticate but never import it; nonzero-work `run`/`resume` loads
  only after manifest, selection, safe path, and checkpoint-state validation.
- Every campaign checkpoint and merge-output CLI path now crosses the same
  relative-path resolver. Absolute, UNC, drive-qualified/drive-relative, ADS,
  traversal, and symlink paths fail before backend construction or output
  creation.
- Final rereview binds the stable precision factory name plus verified module
  SHA-256 into the campaign ID, checkpoint bindings, and every runner-stamped
  stage. Semantic load and merge reject resealed factory provenance or a
  different module claiming the same `BackendIdentity` before work.
- Each stage records the factory identity and then-current available precision
  digits. Resume, validate, and merge permit only capability supersets of prior
  stages; the stable factory binding does not change merely because availability
  grows.
- The CLI reads plugin bytes once, verifies that byte buffer, and compiles and
  executes the same immutable buffer. A path replacement after authentication
  cannot change executed code and the source path is never reopened for import.

## Red/green and final verification

- Initial campaign red failed import because `windows_solver.response_batches`
  did not exist.
- Selection/checkpoint red failed on the deliberately absent runner; merge red
  failed on the absent merge API; precision red produced two errors for the
  absent deep state machine; smoke/path/full/CLI reds failed on their absent
  APIs or commands. Each slice was implemented incrementally to green.
- A final exact-plan regression exposed the absent campaign-source binding;
  adding the current module SHA-256 made it green.
- Rereview reds proved that resealed swapped lineage, sentinel-64-only records,
  inconsistent terminal state, exact capability equality, and the absent
  precision descriptor/path route were all accepted or blocked incorrectly.
  The three scoped remediation slices made those adversarial checks green.
- Final rereview reds proved absent stage factory provenance, same-identity
  module substitution on resume/merge, capability downgrade, and source-path
  TOCTOU. The exact three factory tests now pass.
- Focused command:
  `PYTHONPATH=src:tools python -m unittest tests.test_linear_response_batches tests.test_linear_response_smoke tests.test_linear_response_precision -v`
  — 23/23 passed.
- Full command:
  `PYTHONPATH=src:tools python -m unittest discover -s tests -q`
  — 193/193 passed.
- `python .tasks/validate_board.py` passed in the In Progress state.
- `python -m compileall -q src tools tests` passed.
- `git diff --check` passed.

## Commit SHA(s)

- `dd3d0c41568a8a4853723936d936fe68276a915f` —
  `feat(response): add deterministic B-prime campaign handoff`.
- `f0819b0e3932461748676a4decac81dc01b75c79` —
  `fix(response): harden campaign evidence orchestration`.
- `68f6d4323420587bf70161c9ea8ea7566fc3b6cd` —
  `fix(response): bind campaign precision factory provenance`.

## Self-review and concerns

- The complete exact-553 positive admission path was deliberately not executed:
  doing so would violate the build-only boundary. Its strict structural contract
  and partial rejection are covered without physical work.
- The exact authenticated GSN cache SHA-256 remains
  `0c49fe4c2839444422b2d0ebcf08c912ee06d7e60ed398c9b360ed4c151f28d3`,
  but those bytes are absent here. Native selected execution therefore fails
  before determinant work.
- The installed native adapter authenticates binary64 only. The operator must
  provide separately authenticated 80/120-digit execution and diagnostic
  capability for deep production leaves; the software records missing stages
  instead of fabricating or downgrading them.
- Planning and software readiness do not constitute operator evidence. All 553
  leaves remain uncomputed, no scientific atlas exists, and provider admission
  remains unavailable.
